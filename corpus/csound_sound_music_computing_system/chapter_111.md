# 2. A control instrument that will dynamically spawn the readers and exit. Different

2. A control instrument that will dynamically spawn the readers and exit. Different
instances can start groups of readers.